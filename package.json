{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint:script": "yarn workspace @synbydesign/website lint:script",
    "build": "yarn workspace @synbydesign/website build",
    "format": "pretty-quick",
    "build:netlify": "netlify build"
  },
  "devDependencies": {
    "husky": "^4.2.3",
    "netlify-cli": "^2.52.0",
    "prettier": "^2.0.2",
    "pretty-quick": "^2.0.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged"
    }
  }
}
