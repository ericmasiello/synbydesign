import { TYPE_SIZE } from '../styles/vars';
import makeTypeComponent from '../utils/makeTypeComponent';

const result = makeTypeComponent(TYPE_SIZE.t2);

export const Type2 = result.Type;
const StyleType2 = result.StyledType;
export default StyleType2;
