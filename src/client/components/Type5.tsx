import { TYPE_SIZE } from '../styles/vars';
import makeTypeComponent from '../utils/makeTypeComponent';

const result = makeTypeComponent(TYPE_SIZE.t5);

export const Type5 = result.Type;
const StyleType5 = result.StyledType;
export default StyleType5;
